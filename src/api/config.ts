const BASE = ' https://gigantic-weight-production.up.railway.app';
const AUTH = `${BASE}/auth`;
const USERS = `${BASE}/users`;
const BOARDS = `${BASE}/boards`;
const BOARDS_SET = `${BASE}/boardsSet`;

export { BASE, AUTH, USERS, BOARDS, BOARDS_SET };
